@{
    var currentStep = (int)(ViewBag.CurrentStep ?? 1);

    // Step 3 (Venue Zones) is removed - wizard now has 6 steps:
    // 1: Event Info, 2: Venue, 3: Ticket Types, 4: Seats (preview), 5: Map, 6: Review
    var steps = new[]
    {
        "Event Info",
        "Venue",
        "Ticket Types",
        "Seats",
        "Map",
        "Review"
    };
}

<div class="wizard-progress-line">
    @for (var i = 0; i < steps.Length; i++)
    {
        var stepNumber = i + 1;
        var cssClass = "";

        if (stepNumber < currentStep)
        {
            cssClass = "done";
        }
        else if (stepNumber == currentStep)
        {
            cssClass = "active";
        }

        <div class="wizard-progress-item @cssClass">
            <div class="wizard-circle">
                @(stepNumber < currentStep ? "✓" : stepNumber.ToString())
            </div>

            <div class="wizard-label">
                @steps[i]
            </div>
        </div>
    }
</div>
